const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
  viewContent: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 15,
  },
  textContent: {
    fontSize: 16,
    color: "#000",
    marginLeft: 30,
  },
});
export default styles;
